name: Cataclysm Windows build

on:
  push:
    branches:
    - master
    paths-ignore:
    - 'android/**'
    - 'build-data/osx/**'
    - 'doc/**'
    - 'doxygen_doc/**'
    - 'gfx/**'
    - 'lang/**'
    - 'lgtm/**'
    - 'tools/**'
    - '!tools/format/**'
    - 'utilities/**'
  pull_request:
    branches:
    - master
    paths-ignore:
    - 'android/**'
    - 'build-data/osx/**'
    - 'doc/**'
    - 'doxygen_doc/**'
    - 'gfx/**'
    - 'lang/**'
    - 'lgtm/**'
    - 'tools/**'
    - '!tools/format/**'
    - 'utilities/**'
    types: [opened, reopened, synchronize, ready_for_review]
  workflow_dispatch:
    inputs:
      run_tests:
        description: Run the broad C++ test suite after compiling
        required: false
        type: boolean
        default: true

permissions:
  contents: read

# We only care about the latest revision of a PR, so cancel previous instances.
concurrency:
  group: msvc-build-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

env:
  # Enable pretty backtraces
  BACKTRACE: 1
  # Compressed size ~1GB based on observations
  CCACHE_LIMIT: 10GB
  CDDA_CCACHE_PATH: ${{ github.workspace }}\ccache\
  CDDA_USE_CCACHE: true
  # There's not enough disk space to build both release and debug versions of
  # our dependencies, so we hack the triplet file to build only release versions
  # Have to use github.workspace because runner namespace isn't available yet.
  VCPKG_OVERLAY_TRIPLETS: ${{ github.workspace }}\.github\vcpkg_triplets
  # Also conveniently disable ccache internal compression
  CCACHE_HARDLINK: true
  ZSTD_CLEVEL: 17

jobs:
  build-translations:
    uses: ./.github/workflows/build-translations.yml
    permissions:
      actions: read
      contents: read
    secrets:
      TX_TOKEN: ${{ github.event_name != 'pull_request' && secrets.TX_TOKEN || '' }}

  build_catatclysm:
    name: Build
    needs: build-translations
    runs-on: windows-2022
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false

    - name: Fetch translations artifact into build
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
      with:
        name: translations

    - name: Setup msys2
      uses: msys2/setup-msys2@66cd2cce69caa17b53920067426061ca1de3a884 # v2
      with:
        msystem: mingw64
        install: >-
          gettext
          make

    - name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@30375c66a4eea26614e0d39710365f22f8b0af57 # v3.0.0

    - name: Use GNU tar to enable zstd for actions/cache
      run: |
        echo "Adding GNU tar to PATH"
        echo C:\Program Files\Git\usr\bin>>"%GITHUB_PATH%"
      shell: cmd

    - name: Install stable CMake
      uses: lukka/get-cmake@fffaaafeea488556c2c12dad60690008bc1caacb # latest
      with:
        # 4.0.0 has issues compiling old packages
        cmakeVersion: 3.31.6

    - name: Install vcpkg
      uses: lukka/run-vcpkg@b1a0dd252f06b9e25b3c022a9a03bd7a427fb6a2 # v11.6
      id: runvcpkg
      with:
        vcpkgDirectory: '${{ runner.workspace }}/b/vcpkg'
        vcpkgGitCommitId: 'f6672d8e480ccdecddfad3fd1b838ba369ffe6cd'

    - name: Integrate vcpkg
      run: |
        vcpkg integrate install --vcpkg-root "$env:RUNNER_WORKSPACE\b\vcpkg"
      env:
        RUNNER_WORKSPACE: ${{ runner.workspace }}

    - name: Cache vcpkg installed packages
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: msvc-full-features/vcpkg_installed
        key: vcpkg-installed-sdl2-${{ hashFiles('msvc-full-features/vcpkg.json', '.github/vcpkg_triplets/x64-windows-static.cmake') }}-f6672d8e480ccdecddfad3fd1b838ba369ffe6cd

    - name: Download ccache
      uses: robinraju/release-downloader@28fc21f50d76778e7023361aa1f863e717d3d56f # v1.13
      with:
        repository: 'ccache/ccache'
        tag: 'v4.6.1'
        fileName: 'ccache-4.6.1-windows-x86_64.zip'

    - name: Install ccache
      id: install-ccache
      run: |
        unzip ccache-4.6.1-windows-x86_64.zip
        cp ccache-4.6.1-windows-x86_64/ccache.exe ccache-4.6.1-windows-x86_64/cl.exe
        cp ccache-4.6.1-windows-x86_64/ccache.exe ccache-4.6.1-windows-x86_64/clang-cl.exe
        mv ccache-4.6.1-windows-x86_64 $env:CDDA_CCACHE_PATH

    - name: Get ccache vars
      id: get-vars
      run: |
        echo "datetime=$(/bin/date -u "+%Y%m%d%H%M")" >> $GITHUB_OUTPUT
        echo "datetime-seconds=$(/bin/date +%s)" >> $GITHUB_OUTPUT
        echo "ccache-path=$(echo "$APPDATA\\ccache")" >> $GITHUB_OUTPUT
      shell: bash

    - name: ccache cache files
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: ${{ steps.get-vars.outputs.ccache-path }}
        # double-dash after compiler is not a typo, it is to disambiguate between g++-<date> and g++-11-<date> for restore key prefix matching
        key: ccache-${{ github.ref_name }}-${{ runner.os }}-msvc--${{ steps.get-vars.outputs.datetime }}
        restore-keys: |
          ccache-master-${{ runner.os }}-msvc--

    - name: Configure ccache
      run: |
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -M 10G
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -s -v
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -z

    - name: Symlink intermediates to C
      run: |
        mkdir -p "$env:TEMP\objwin"
        New-Item -ItemType SymbolicLink -Target "$env:TEMP\objwin" objwin

    - name: Build
      env:
        CDDA_RELEASE_BUILD: 1
      run: |
          msbuild -m -p:Configuration=Release -p:Platform=x64 -p:UseSDL3=false "-target:Cataclysm-vcpkg-static;Cataclysm-test-vcpkg-static;JsonFormatter-vcpkg-static;zzip" msvc-full-features/Cataclysm-vcpkg-static.sln

    - name: Get ccache cache age cutoff
      id: get-cache-age
      run: |
        NOW=`/bin/date +%s`
        DELTA=$(( $NOW - ${STEPS_GET_VARS_OUTPUTS_DATETIME_SECONDS} ))
        echo "ccache-age=${DELTA}" >> $GITHUB_OUTPUT
      shell: bash
      env:
        STEPS_GET_VARS_OUTPUTS_DATETIME_SECONDS: ${{ steps.get-vars.outputs.datetime-seconds }}

    - name: Post-build ccache manipulation
      if: ${{ !failure() }}
      run: |
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -s -v
        & "$env:CDDA_CCACHE_PATH\ccache.exe" --evict-older-than ${env:STEPS_GET_CACHE_AGE_OUTPUTS_CCACHE_AGE}s
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -s -v
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -M ${{ env.CCACHE_LIMIT }}
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -c
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -s -v
      env:
        STEPS_GET_CACHE_AGE_OUTPUTS_CCACHE_AGE: ${{ steps.get-cache-age.outputs.ccache-age }}

    - name: clear ccache on PRs
      if: ${{ github.ref_name != 'master' }}
      run: |
        & "$env:CDDA_CCACHE_PATH\ccache.exe" -C

    - name: Dump vcpkg logs if build failed
      if: failure()
      run: |
        echo =================================================
        Get-ChildItem "$env:GITHUB_WORKSPACE\msvc-full-features\vcpkg_installed" -Recurse
        echo =================================================
        Get-ChildItem "$env:RUNNER_WORKSPACE/b/vcpkg/buildtrees" |
        Foreach-Object {
            Get-ChildItem $_.FullName -Filter *.log |
            Foreach-Object {
              echo =================================================
              echo $_.FullName
              echo =================================================
              type $_.FullName
            }
        }
      env:
        RUNNER_WORKSPACE: ${{ runner.workspace }}

    - name: Compile .mo files for localization
      if: ${{ github.ref_name != 'master' }}
      shell: msys2 {0}
      run: |
          make -C lang -j2
          mkdir -p ./data/mods/TEST_DATA/lang/mo/ru/LC_MESSAGES
          msgfmt -f -o ./data/mods/TEST_DATA/lang/mo/ru/LC_MESSAGES/TEST_DATA.mo ./data/mods/TEST_DATA/lang/po/ru.po

    - name: Enable GitHub Actions problem matchers
      if: ${{ github.ref_name != 'master' }}
      run: |
          Write-Output "::add-matcher::build-scripts/problem-matchers/catch2.json"
          Write-Output "::add-matcher::build-scripts/problem-matchers/debugmsg.json"

    - name: Run tests
      if: ${{ github.event_name == 'workflow_dispatch' && inputs.run_tests == true }}
      run: |
          .\Cataclysm-test-vcpkg-static-Release-x64.exe --min-duration 20 --rng-seed time --order lex --wait-for-keypress exit

    - name: Dump disk usage logs if job failed
      if: failure()
      run: |
        echo =================================================
        df
        echo =================================================
        du -sh *
        echo =================================================
        du -sh msvc-full-features/*
        echo =================================================
        du -sh tools/*
        echo =================================================
      shell: bash

    - name: Don't upload vcpkg cache on failure or PRs
      if: ${{ failure() || cancelled() || github.ref_name != 'master' }}
      run: |
        echo "RUNVCPKG_NO_CACHE=1" >> $GITHUB_ENV
      shell: bash
    - name: package PR Windows tiles x64 artifact
      id: package-pr-windows-tiles-x64
      if: ${{ github.event_name == 'pull_request' && github.event.pull_request.draft == false && success() }}
      continue-on-error: true
      env:
        PR_NUMBER: ${{ github.event.pull_request.number }}
      shell: pwsh
      run: |
        @"
        build type: windows-tiles-x64
        build number: $env:GITHUB_RUN_ID
        commit sha: $env:GITHUB_SHA
        commit url: https://github.com/$env:GITHUB_REPOSITORY/commit/$env:GITHUB_SHA
        "@ | Set-Content -Path VERSION.txt
        .\build-scripts\windist.ps1
        $zips = @(Get-ChildItem -File -Filter 'cataclysmdda-0.J.zip')
        if ($zips.Count -ne 1) {
            Write-Error "Expected exactly one release zip, found $($zips.Count)"
            exit 1
        }
        Copy-Item -LiteralPath $zips[0].FullName -Destination "ccb-pr-windows-tiles-x64-$env:PR_NUMBER.zip"
    - name: upload PR Windows tiles x64 artifact
      id: upload-pr-windows-tiles-x64
      if: ${{ steps.package-pr-windows-tiles-x64.outcome == 'success' }}
      continue-on-error: true
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: ccb-pr-windows-tiles-x64-pr-${{ github.event.pull_request.number }}
        path: ccb-pr-windows-tiles-x64-${{ github.event.pull_request.number }}.zip
        if-no-files-found: error
        retention-days: 14
