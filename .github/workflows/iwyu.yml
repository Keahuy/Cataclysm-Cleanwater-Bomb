name: IWYU (include-what-you-use)
on:
  pull_request:
    branches:
    - master
    types: [opened, reopened, synchronize, ready_for_review]
    paths:
    - 'src/**'
    - 'tests/**'
    - 'tools/iwyu/**'
    - 'build-scripts/ci-iwyu-run.py'
    - 'build-scripts/get_affected_files.py'
    - 'CMakeLists.txt'
    - '**/CMakeLists.txt'
    - '.github/workflows/iwyu.yml'
  # Full-repository IWYU is intentionally manual.  The repository has a
  # pre-existing full-scan baseline; PRs use the bounded affected-file path.
  workflow_dispatch:

# We only care about the latest revision of a PR, so cancel all previous instances.
concurrency:
  group: iwyu-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true
jobs:
  check-changes:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ github.event_name != 'pull_request' || steps.changed-files.outputs.result == 'true' }}
    steps:
    - name: check for relevant file changes
      id: changed-files
      if: ${{ github.event_name == 'pull_request' }}
      uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
      with:
        script: |
          const response = await github.paginate(github.rest.pulls.listFiles,
            {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            }
          );
          const dominated_by = [
            /^src\//,
            /^tests\//,
            /^tools\/iwyu\//,
            /^build-scripts\/ci-iwyu-run\.py$/,
            /^build-scripts\/get_affected_files\.py$/,
            /CMakeLists\.txt$/,
            /^\.github\/workflows\/iwyu\.yml$/,
          ];
          const is_agent_instruction = f => /(^|\/)AGENTS\.md$/.test(f.filename);
          const dominated = response.some(f =>
            !is_agent_instruction(f) && dominated_by.some(r => r.test(f.filename))
          );
          return dominated;

  iwyu:
    needs: check-changes
    outputs:
      has_errors: ${{ steps.run_iwyu.outputs.has_errors }}
    if: ${{ needs.check-changes.outputs.should_run == 'true' }}
    runs-on: ubuntu-24.04
    env:
        COMPILER: clang++-19
    steps:
    - name: install LLVM 19
      run: |
          sudo apt install llvm-19-dev clang-19 libclang-19-dev
          sudo apt install cmake
    - name: checkout IWYU repository
      id: iwyu-checkout
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        repository: include-what-you-use/include-what-you-use
        path: include-what-you-use-src
        ref: clang_19
        persist-credentials: false
    - name: checkout own repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        path: Cataclysm-DDA
        persist-credentials: false
    - name: cache IWYU build
      id: iwyu-cache
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: iwyu-build
        key: iwyu-clang19-ubuntu2404-${{ steps.iwyu-checkout.outputs.commit }}
    - name: build IWYU
      if: steps.iwyu-cache.outputs.cache-hit != 'true'
      run: |
        cmake -B iwyu-build -DCMAKE_PREFIX_PATH=/usr/lib/llvm-19 -DCMAKE_BUILD_TYPE=Release include-what-you-use-src
        cmake --build iwyu-build --parallel 4
    - name: set IWYU paths
      id: build-iwyu
      run: |
        echo "IWYU_SRC_DIR=${PWD}/include-what-you-use-src">> "$GITHUB_OUTPUT"
        echo "IWYU_BIN_DIR=${PWD}/iwyu-build/bin">> "$GITHUB_OUTPUT"
    - name: determine changed files
      if: ${{ github.event_name == 'pull_request' }}
      uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
      with:
        script: |
          var fs = require('fs');
          const response = await github.paginate(github.rest.pulls.listFiles,
            {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            }
          );
          const files = response.map(x => x.filename);
          for (const path of files) {
            console.log(path);
          }
          fs.writeFileSync("Cataclysm-DDA/files_changed", files.join('\n') + '\n');
    - name: create CDDA compilation database
      working-directory: Cataclysm-DDA
      run: |
        set -x
        echo ::group::Building compilation database
        cmake -B build \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DCMAKE_CXX_COMPILER=$COMPILER \
            -DCMAKE_BUILD_TYPE="Release" \
            -DTILES=${TILES:-0} \
            -DSOUND=${SOUND:-0} \
            -DLOCALIZE=${LOCALIZE:-0} \
            .
        echo ::endgroup::

        # and include database too, for get_affected_files.py
        make includes -j4 --silent TILES=${TILES:-0} SOUND=${SOUND:-0} LOCALIZE=${LOCALIZE:-0}
    - uses: ammaraskar/gcc-problem-matcher@0f9c86f9e693db67dacf53986e1674de5f2e5f28 # master
    - name: run IWYU
      id: run_iwyu
      working-directory: Cataclysm-DDA
      env:
        IWYU_SRC_DIR: ${{ steps.build-iwyu.outputs.IWYU_SRC_DIR }}
        IWYU_BIN_DIR: ${{ steps.build-iwyu.outputs.IWYU_BIN_DIR }}
      run: |
        set +e
        PATH="${PATH}:${IWYU_BIN_DIR}:${IWYU_SRC_DIR}"
        python build-scripts/ci-iwyu-run.py
        exit_code=$?
        echo "has_errors=$exit_code" >> $GITHUB_OUTPUT
        exit $exit_code
    - name: Create pr comment artifacts
      if: ${{ failure() && github.event_name == 'pull_request' && steps.run_iwyu.outputs.has_errors != 0 }}
      working-directory: Cataclysm-DDA
      run: |
        set +e
        cat files_changed | while read f; do
          if [ -e "$f" ]; then
            git add -- "$f"
          fi
        done
        git checkout .
        git reset
        git diff --exit-code
        if [ $? -ne 0 ]; then
          mkdir -p ../suggestions
          git diff -U1 > ../suggestions/diff.txt
          echo ${{ github.event.pull_request.number }} > ../suggestions/pr_number
          echo ${{ github.event.pull_request.head.sha }} > ../suggestions/commit_sha
          echo '[Include What You Use](https://github.com/CleverRaven/Cataclysm-DDA/blob/main/doc/c%2B%2B/DEVELOPER_TOOLING.md#include-what-you-use)' > ../suggestions/comment.txt
        fi
        exit 0 # Always 'succeed' this step to reduce job noise.
    - name: Upload pr comment artifacts
      if: ${{ failure() && github.event_name == 'pull_request' && steps.run_iwyu.outputs.has_errors != 0 }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: suggestions
        path: suggestions/


  iwyu-result:
    if: ${{ always() && !cancelled() }}
    needs: iwyu
    runs-on: ubuntu-latest
    steps:
    - name: require successful IWYU
      env:
        JOB_RESULT: ${{ needs.iwyu.result }}
        JOB_ERRORS: ${{ needs.iwyu.outputs.has_errors }}
      run: |
        if [[ "$JOB_RESULT" == "skipped" ]]; then
          echo "IWYU result: skipped"
          exit 0
        fi
        if [[ "$JOB_RESULT" == "success" && "$JOB_ERRORS" == "0" ]]; then
          echo "IWYU result: success"
          exit 0
        fi
        echo "IWYU failed with result: $JOB_RESULT"
        exit 1
